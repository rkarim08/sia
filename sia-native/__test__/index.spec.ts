import { describe, expect, it } from "vitest";

describe("@sia/native comparison tests", () => {
  it.todo("astDiff: native vs TypeScript produce same results");
  it.todo("graphCompute(pagerank): native vs TypeScript produce same scores");
  it.todo(
    "graphCompute(shortest_path): native vs TypeScript produce same distances",
  );
  it.todo(
    "graphCompute(connected_components): native vs TypeScript produce same groups",
  );
});
